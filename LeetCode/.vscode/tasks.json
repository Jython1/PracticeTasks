{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build-and-run",
            "type": "shell",
            "command": "cmake --build build && ./build/program",
            "problemMatcher": []
        }
    ]
}
